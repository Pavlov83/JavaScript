//aggregation is a special form of association relationship where an object has a more important role than the others:

var company = {
    name:"Acme inc.",
    employees:[]
};

var johnSmith = new personalbar("John","Smith");
var marioRossi = new personalbar("Mario","Rossi");

company.employees.push(johnSmith);//pushed into employees array
company.employees.push(mario.Rossi);