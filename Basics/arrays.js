var colors = ['red','blue','green'];
alert(colors[1]);

//we can use constructor and creat array :
var colors = new Array('red','yellow','blue');

//we can add elements:
colors[3] = 'green';
colors.push('purple');

//we can get the amount of indexes in an Array
var numbers[5,77,6,'Seven',1,55];
alert(numbers.length);
// we can reverse the Array
alert(numbers.reverse);

alert(colors);
