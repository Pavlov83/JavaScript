calculateAge(1990);


function calculateAge(year){
    console.log(2016-year);
}

//Hoisting works only for funcion declarations and not for function expressions(when the function is stored referenced with variable)
